Run:
make
./test OR ./unittest

PQ Insert - inserts item into heap at last index, swaps with first index, then heapifies first index down.
PQ Max Dequeue - removes max item in heap, swaps with first index, then heapifies first index down.
